# Teams Manifest Notes

1. Replace `00000000-0000-0000-0000-000000000000` in `manifest.json` with the Azure Bot App ID.
2. Add icons:
   - `color.png` (192x192)
   - `outline.png` (32x32)
3. Zip `manifest.json`, `color.png`, and `outline.png` and upload to Teams.
